<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ApocalipsisGranada">

        <!-- 🟢 Pantalla principal (Inicio automático al abrir la app) -->
        <activity
            android:name=".vista.PrincipalActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🟨 Pantallas secundarias -->
        <activity
            android:name=".vista.VistaGuia"
            android:exported="false" />
        <activity
            android:name=".vista.VistaHistorial"
            android:exported="false" />
        <activity
            android:name=".vista.VistaServicios"
            android:exported="false" />

        <!-- 🔐 Pantalla de login (ya no es la principal) -->
        <activity
            android:name=".vista.LoginActivity"
            android:exported="false" />

    </application>
</manifest>
